# Grinding leet code problems

Solving leet code problems - just for the heck of it.
The code inside `/src` is pure, hand written without help of any AI.

## Execute

To execute individual solutions use:

`npx tsx src/<problem-name>`
